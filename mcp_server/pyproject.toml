[project]
name = "data-steward-mcp"
version = "0.2.0"
description = "Lambda-native MCP server exposing OpenMetadata operations for a Data Steward assistant"
requires-python = ">=3.12"

[tool.pytest.ini_options]
testpaths = ["tests"]
python_files = ["test_*.py"]
addopts = "-ra --strict-markers"
