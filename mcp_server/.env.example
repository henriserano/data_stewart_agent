# Local development only. In AWS, secrets are pulled from SSM Parameter Store.
OPENMETADATA_HOST=https://openmetadata.your-domain.com
OPENMETADATA_JWT=eyJhbGciOi...   # OpenMetadata bot JWT
MCP_BEARER_TOKEN=change-me-to-a-long-random-string
